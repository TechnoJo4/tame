# tame

minimal harness for people who don't care to larp as terminal users

features:
- an agent loop
- multi-provider routing
- nothing else

plugins:
- [acp](https://agentclientprotocol.com/) (over tcp/unix socket)
